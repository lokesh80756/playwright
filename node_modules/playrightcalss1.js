const {chromium} = require ('playwright'); // import browser from playwright
(async ()=>{
    const browser = await chromium.launch({headless :false}); // to launch the chromium browser in headed mode
    const page = await browser.newPage();
    await page.goto('https://www.facebook.com');
    const  title =await page .title();
    console.log(title);
    const user = await page.locator("#email");
    await user.fill("lokesh");
    const pass = await page.locator("//input[@data-testid='royal_pass']");
    await pass.fill("uhiqhid");
    const login = await page .locator ("._4jy1");
    await login .click();
})();


